<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Book Review App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Book Review App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addBook">Add new book</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Book</h2></div>
        <br>
        <div class="text-center">
          <% if (book) { %> 
            <form action="/editBook/<%= book.bookId %>" method="POST" enctype="multipart/form-data">
                <label for="bookname">Book Name:</label><br>
                <input type="text" id="bookname" name="bookname" value = "<%= book.name %>" required><br><br>
                
                <label for="image">New Image:</label><br>
                <br> <input type="file" id="image" name="image" accept="image/*" value="<%= book.image %>"><br><br>

                <label for="rating">Rating:</label><br>
                <input type="number" id="rating" name="rating" min="1" step="1" max="10" value = "<%= book.rating %>" required><br><br>

                <label for="review">Review:</label><br>
                <input type="text" id="review" name="review" value = "<%= book.review %>" required><br><br>


                <button type="submit">Update Book</button>
            </form>
          <% } else { %>
              <p>No book found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
